import components.EditorPanel;
import components.HomePanel;
import components.MenuPanel;
import components.temo;

class Driver {
    public static void main(String[] args) {
        /*HomePanel panel1 = new HomePanel(0, 600, 0, 683);
        MenuPanel panel2 = new MenuPanel(0, 600, 0, 683);
        EditorPanel panel3 = new EditorPanel();
        temo temp = new temo(panel1.getHomePanel(), panel2.getMenuPanel());
        
        boolean switchStates = false;
        while (true){
            if (panel1.switchMode()){
                if (!switchStates){
                    temp.setMode(1);
                    switchStates = false;
                }
            }
        }*/

        GUI temp = new GUI();
    }
}